# API key for AI Builders backend (transcription, chat).
# When deployed to ai-builders.space, AI_BUILDER_TOKEN is injected automatically.
# For local dev, use SUPER_MIND_API_KEY (same value as your platform API key).
SUPER_MIND_API_KEY=your_platform_api_key_here
